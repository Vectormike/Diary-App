<% include partials/header.ejs %>

<% posts.forEach((post) => { %>
    <div class="ui container">
        <h2><%= post.title%></h2>
        <img src="<%= post.image%>" alt="">
       
        <a href="/index/<%=post._id%>" class="ui circular button"> Read more <i class="ui icon info"></i></a>
    </div>
<%})%>
<% include partials/footer.ejs %>